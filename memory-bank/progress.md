# Progress

## Current
- Memory-bank files compressed for lower token usage.
- `setup.sh` includes RTK install guard (`command -v rtk || brew install rtk`).

## Backlog
- Consider `Brewfile` for dependency intent.
- Add lightweight validation checks (shell/json).
- Decide Ghostty sync/overwrite behavior.
- Decide Karabiner long-term source model.

## Recent Changes
- 2026-05-10: Added RTK installation step to `setup.sh`.
- 2026-05-10: Compressed memory-bank docs (`projectbrief.md`, `productContext.md`, `systemPatterns.md`, `techContext.md`, `activeContext.md`, `progress.md`).
